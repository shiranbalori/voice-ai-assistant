# Production: full URL to the Render backend API (include /api suffix)
# VITE_API_BASE_URL=https://your-service.onrender.com/api

# Local development: leave unset to use the Vite dev-server proxy at /api
